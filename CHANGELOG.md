# 更新记录

## 2.2.0

- 新增用户主动开启的定时后台刷新，间隔 1–1440 分钟、默认 5；关闭原用量页后仍可按计划读取，也可「立即刷新」或「关闭并撤销授权」。
- 每轮在非活动临时标签页重新加载官方用量页，只采集 DOM 可识别指标；只清理本轮创建且未被用户接管的标签页。浏览器和悬浮球须运行，官方登录须有效。
- 增加 `alarms` 和可选 `https://chatgpt.com/*` 来源权限。仅开启定时模式时请求网站授权，拒绝仍能当前页面读取；凭据留在官方浏览器，不读取 Cookie、不保存账号秘密、不调用私有用量接口。
- 配对及定时启用状态只存于会话，长期只保存间隔数字；重载或浏览器重启后重新配对并开启。失败或未识别不更新成功时间，不覆盖上次有效读数。
- 扩展目录校验支持旧/新完整清单并保留迁移备份，拒绝混合和更宽权限。旧桌面可能显示扩展目录更新警告但不会覆盖新版文件；可先 `npm run extension:update`，无需重装桌面。

此版本源码提交不代表安装包已发布。后台调度与合成测试不代替真实 Chrome / Edge、官方账号及 Windows 安装验收。桌面 3 分钟历史提示独立于刷新间隔，页面读取时间不等于官方后台更新时间。

## 2.1.3

- 支持用户实际可访问的 `https://chatgpt.com/settings/usage?tab=overview` 用量页，并保留旧 `/codex/settings/usage` 路径兼容。
- 同步修正后台校验、页面采集与解析器的地址限制；仍仅接受 `https://chatgpt.com` 下的两个精确用量路径。
- 扩展、桌面入口与使用说明统一指向新地址；浏览器权限、本机配对和数字字段限制保持不变。
- 已有源码仓库可通过 `npm run extension:update` 更新固定扩展目录；旧桌面程序的入口需等桌面版本更新后生效，期间使用扩展中的新链接。

此修复解决页面地址被拒绝的问题。新页面的真实指标布局尚未验收，无法识别的字段继续显示未知。

## 2.1.2

- 修复扩展配对码输入框随页面识别状态一起禁用、无法输入的问题；页面采集仍校验官方用量页与临时授权。
- 新增 `npm run extension:update`：在 Windows 将已取得的源码扩展安全同步到固定目录，随后由用户手动重载并重新配对。无需重装桌面程序或安装 npm 依赖。
- 保持扩展身份、浏览器权限、发布仓库与签名公钥不变；旧桌面程序不会覆盖固定目录中更高版本的扩展。

本地与合成测试不等同于用户实际 Chrome / Edge 工具栏及真实账号页面验收。

## 2.1.1

- 修复 Windows 打包时使用系统默认编码读取中文扩展清单而导致的 UnicodeDecodeError；所有打包配置显式按 UTF-8 读取。
- Windows 持续集成增加源码与扩展 ZIP 的真实归档步骤，覆盖正式发布使用的命令。
- 沿用已配置的 icjunge/gpt-orb-safe 发布源和 Ed25519 公钥，桌面与扩展版本统一为 2.1.1。
- 2.1.0 首次发布构建停在扩展归档步骤，未生成正式 Release；保留原标签，用新补丁版本继续发布。

Windows 实机安装升级与真实账号页面验收仍需完成；构建通过不代表这些验收已完成。

## 2.1.0

- 新增安装版更新：启动后与每 6 小时检查，签名验证后下载，由用户点击「重启并更新」。
- 更新来源与 Ed25519 公钥随程序固定；校验版本、安装包长度、SHA-256 和 SHA-512，拒绝降级及被篡改的文件。
- 安装前再次校验文件并备份本地显示设置。更新发布源尚未配置时保持停用。
- 浏览器扩展迁移到用户数据中的固定目录，完整替换后可在扩展内重新加载，无须每次删除再安装；重载后需要重新配对。
- 加入 GitHub Actions 测试、Windows 安装包构建与独立签名发布工作流。

旧 ZIP 版需要一次迁移安装。Windows 实机安装升级与真实账号页面尚未完成验收；当前版本没有配置正式发布源。

## 2.0.1

- 修复浏览器扩展消息 API 不可用时的未处理错误，显示正确的扩展加载指引。
- 保持原有扩展权限、身份及本机来源校验。
