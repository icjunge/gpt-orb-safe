# 更新记录

## 2.7.0

- 新增 macOS 13 及以上支持，提供 Apple 芯片 arm64 与 Intel x64 两种 DMG；拖入应用程序后即可启动，普通用户无需安装 Node.js、Git 或执行命令。
- Mac 一键连接自动准备对应芯片的固定官方 Codex 组件，分别校验归档与可执行文件；登录凭据由官方组件存入 macOS 钥匙串，独立登录目录不覆盖已有 CLI 账号。
- 增加菜单栏图标、⌘⌥G 快捷键、系统登录启动设置与原生毛玻璃详情面板；保留小尺寸百分比圆球、悬停放大及固定窗口拖动逻辑。
- Mac 按芯片类型检查经过 Ed25519 验签的更新描述，用户点击后打开官方 DMG 下载；首版仅为 ad-hoc 签名、未经 Apple Developer ID 签名及公证，需手动替换应用，不宣称静默安装或已校验浏览器下载内容。Windows 原签名自动更新保持不变。
- 增加双架构原生 Mac 构建、签名与隔离启动检查，覆盖生产界面、固定窗口长按、官方组件缓存和未登录账号协议；没有执行真实账号登录或模型任务。

## 2.6.0

- 新增「连接 Codex」一键设置：自动准备固定版本的官方 Windows x64 组件，打开官方网页登录，完成后立即读取额度并按设定周期刷新；普通用户不再需要安装 Node.js、Git 或运行终端命令。
- 首次连接下载约 85 MB 的官方组件并独立缓存；压缩包与可执行文件分别校验固定长度和 SHA-256，后续应用更新不重复携带该组件。
- 自动配置使用独立登录目录与 Windows 系统凭据库，不覆盖已有 Codex 登录，不回退明文凭据；正常 Codex 账号授权不等于只读 OAuth 权限，工具自身仅执行固定连接与查询操作。
- 增加下载进度、取消、重试和退出此账号；取消及账号切换会等待相关后台进程结束，防止迟到回调重新保存登录状态。清理失败明确提示，可再次退出。
- 老用户保留原有 CLI 连接及刷新设置；高级设置可选择自动配置。组件缺失或登录失效可重新连接，未知 Token 继续隐藏。
- 保留 2.5.4 的悬浮球外观、固定窗口与拖动位置修复，沿用原签名更新源和公钥。
- 新增隔离目录下的真实 Windows 官方组件下载、缓存与账号协议检查；不使用真实账号完成登录，也不发起模型任务。

## 2.5.4

- 悬停放大改为固定 56 像素透明窗口内的 48/56 像素圆球绘制；悬停、按下和松开不再触发原生窗口缩放，减少 Windows 窗口异步尺寸变化与鼠标输入的耦合。
- 位置由应用维护的锚点及实际拖动目标决定，不再把松手时系统返回的临时窗口位置保存为新位置；旧版圆球位置按可见圆面的坐标兼容。
- 保留 2.5.3 的小字号、玻璃边缘、透明圆面与签名更新方式。
- 新增隔离配置下的 Windows 真实系统鼠标回归检查，覆盖悬停、长按、松开和实际拖动；不读取账号或启动用量查询。

## 2.5.3

- 修复长按位置漂移的入口：拖动阈值与位置计算统一使用主进程屏幕坐标；仅真实拖动才保存新锚点，微抖、重复按下和异步窗口位置回报不再自动变成已保存的位移。
- 松手由主进程返回是否拖动，点击仅在未拖动时打开面板；取消、失去捕获和迟到的手势响应不会误触点击。
- 缩小百分比字号，增加圆球内部留白；48/56 像素圆球尺寸及仅百分比布局不变。
- 增加圆形玻璃边缘的高光、暗部轮廓和内侧柔和阴影，让浅色及深色桌面都更容易辨认。中心保持半透明，圆外保持透明；不启用圆球 Acrylic 或桌面采集。
- 保留原有账号读取与签名更新配置，已有安装版直接应用内更新。

## 2.5.2

- 修复圆球后方的矩形底色：圆球始终使用透明窗口，并显式关闭系统背景底材；不再根据 GPU 状态启用圆球 Acrylic。详情面板材质保持独立。
- 圆球由 64 像素缩小至 48 像素，悬停仅轻微放大至 56 像素，球内始终只有剩余百分比（无数据为 —），移除圆环、额度名称和倒计时。
- 额度窗口、数据来源、历史状态及重置时间移入鼠标悬停提示；点击、拖动和键盘操作保持可用。
- 本地内置可分发的 Inter 字体，调整数字字重、字距与 100% 留白；Apple 系统沿用系统字体。没有打包 Apple SF Pro。
- 沿用现有签名更新源与公钥，现有安装版可在应用内升级。圆球是半透明效果，不宣称具有跨应用背景模糊。

## 2.5.1

- 悬浮球启动时等待有界的 GPU 状态结果；仅支持的 Windows 11 与硬件合成组合请求系统 Acrylic。软件合成、未知能力和较旧系统采用透明圆球，显式关闭默认窗口底材，避免灰色方块。
- 高对比度与减少透明度时使用清晰实色圆球；原生背景是否模糊仍受 Windows 控制，API 成功不作为视觉证明。
- 包含下列 2.5.0 交互与更新能力。2.5.0 候选未正式发布，保留原标签，不作为稳定更新。

## 2.5.0（未发布候选）

- 悬浮球平时只显示剩余百分比；鼠标悬停或键盘聚焦后从 64 放大至 104 像素，显示额度名称与重置倒计时。离开延迟收起，拖动时保持尺寸。
- Windows 11 22H2 及以上的悬浮球改用普通原生窗口、Acrylic 与圆形区域裁切；避免整窗不透明度影响系统背景，玻璃着色独立于文字。
- 重新按实际尺寸处理边缘、负坐标显示器和缩放变化；不通过鼠标悬停抢占其他应用的焦点。
- 版本提交可触发受保护的签名发布流程；保持原签名公钥，已有 2.4.1 安装版可直接检查更新并重启升级。
- 未增加账号、凭据或产品屏幕采集权限。原生模糊受 Windows 合成器与透明效果设置控制，网页预览不作为实体 Windows 验证。

## 2.4.1

- 概览改为连续玻璃面板，主要额度并排展示；移除额度卡片外框与底色，额外额度采用紧凑行。
- 面板收窄至 340 像素，内容自适应高度下限降至 240；长名称和更多额度仍可完整查看。
- 顶部精简为设置和收起，刷新移到底部；读取时间去掉重复文字，异常状态保持可见。
- Token 与重置次数无数据时隐藏，保留有效的 0、手动折叠与所有连接设置。
- 沿用系统原生背景和原有安全边界，不新增账号或屏幕采集权限。

## 2.4.0

- 概览删除重复的大数字卡片，各额度仅显示一次；来源、刷新配置、安装和说明统一移入设置，顶栏可直接刷新。
- 面板高度随额度与展开内容调整，两个窗口时自动缩短；设置独立展示。
- 未提供的 Token、重置次数不占用概览空间；可用的 0 正常显示，Token 保留手动展开和收起。
- 去掉高不透明度的多层浅色底板；原生材质上只叠低浓度中性染色，设置可调 0–70%，默认 16%，文字保持不透明。
- 使用与白字暗色界面一致的应用内原生暗色主题；区分系统材质已请求、不支持、减少透明和不可用，避免把 API 未抛错误写成视觉验证成功。
- 额度查询、凭据及签名更新边界不变。原生背景由 Windows 决定，实机视觉仍需确认；不模拟或采集桌面截图。

## 2.3.1

- 重做浅色玻璃质感面板与悬浮球，放大关键读数，精简文案，额度先于连接设置展示。
- Token 未提供时自动折叠，恢复有效数据后展开；部分可用时仅展示有效项，0 不会被误判为空。保留手动展开/收起。
- Windows 11 22H2 及以上的详情面板尝试使用系统亚克力背景，其他平台与受限环境保留清晰底色；响应系统高对比度与减少透明度偏好。
- 保留现有 Codex 查询、凭据边界和签名更新信任配置。此更新不新增账号权限。

原生 Windows 背景及实体安装体验仍需实机确认；构建包和源码提交不等于签名 Release 已正式发布。

## 2.3.0

- 新增官方 Codex CLI 本机来源，用户主动开启后通过 stdio App Server 定时查询；不打开临时页面，也不需要浏览器保持运行。间隔 1–1440 分钟、默认 5，启用及间隔偏好保留，支持立即刷新和停止读取。
- 展示官方实际返回的独立额度窗口、重置时间，以及响应提供时的剩余重置次数。不自行恢复额度，不执行额度重置，不编造 Spark 窗口。
- 协议支持时读取服务端 Token 累计及最新日桶，明确统计来源；不扫描本机日志，不把日桶写成本机今日总量。字段未知或可选统计不支持时保持未知。
- 官方 Codex 管理本机登录与续期；悬浮球不读取 / 复制凭据文件，不记录原始 RPC，不发起模型任务。界面提供复制安装 / 登录命令和打开官方帮助，安装登录由用户执行。
- 浏览器模式作为手动选择的备用来源保留。两种来源不自动混用，普通失败保留最近一次有效记录及成功时间；检测到退出登录或账号变化时清除旧账号记录。
- 桌面为新增能力的入口；只执行 extension:update 无法给旧桌面启用本机 Codex。沿用原有签名更新、公钥和发布保护，不更换信任锚点。

本机模式需用户已安装官方 Codex CLI，并以同一 ChatGPT 账号登录。CLI 自身可能读写续期凭据，存储方式取决于既有官方配置。App Server 仍属实验性；自动测试与构建不替代真实 Windows、CLI 登录及账号额度验收。源码更新不代表对应安装包已发布。

## 2.2.0

- 新增用户主动开启的定时后台刷新，间隔 1–1440 分钟、默认 5；关闭原用量页后仍可按计划读取，也可「立即刷新」或「关闭并撤销授权」。
- 每轮在非活动临时标签页重新加载官方用量页，只采集 DOM 可识别指标；只清理本轮创建且未被用户接管的标签页。浏览器和悬浮球须运行，官方登录须有效。
- 增加 `alarms` 和可选 `https://chatgpt.com/*` 来源权限。仅开启定时模式时请求网站授权，拒绝仍能当前页面读取；凭据留在官方浏览器，不读取 Cookie、不保存账号秘密、不调用私有用量接口。
- 配对及定时启用状态只存于会话，长期只保存间隔数字；重载或浏览器重启后重新配对并开启。失败或未识别不更新成功时间，不覆盖上次有效读数。
- 扩展目录校验支持旧/新完整清单并保留迁移备份，拒绝混合和更宽权限。旧桌面可能显示扩展目录更新警告但不会覆盖新版文件；可先 `npm run extension:update`，无需重装桌面。

此版本源码提交不代表安装包已发布。后台调度与合成测试不代替真实 Chrome / Edge、官方账号及 Windows 安装验收。桌面 3 分钟历史提示独立于刷新间隔，页面读取时间不等于官方后台更新时间。

## 2.1.3

- 支持用户实际可访问的 `https://chatgpt.com/settings/usage?tab=overview` 用量页，并保留旧 `/codex/settings/usage` 路径兼容。
- 同步修正后台校验、页面采集与解析器的地址限制；仍仅接受 `https://chatgpt.com` 下的两个精确用量路径。
- 扩展、桌面入口与使用说明统一指向新地址；浏览器权限、本机配对和数字字段限制保持不变。
- 已有源码仓库可通过 `npm run extension:update` 更新固定扩展目录；旧桌面程序的入口需等桌面版本更新后生效，期间使用扩展中的新链接。

此修复解决页面地址被拒绝的问题。新页面的真实指标布局尚未验收，无法识别的字段继续显示未知。

## 2.1.2

- 修复扩展配对码输入框随页面识别状态一起禁用、无法输入的问题；页面采集仍校验官方用量页与临时授权。
- 新增 `npm run extension:update`：在 Windows 将已取得的源码扩展安全同步到固定目录，随后由用户手动重载并重新配对。无需重装桌面程序或安装 npm 依赖。
- 保持扩展身份、浏览器权限、发布仓库与签名公钥不变；旧桌面程序不会覆盖固定目录中更高版本的扩展。

本地与合成测试不等同于用户实际 Chrome / Edge 工具栏及真实账号页面验收。

## 2.1.1

- 修复 Windows 打包时使用系统默认编码读取中文扩展清单而导致的 UnicodeDecodeError；所有打包配置显式按 UTF-8 读取。
- Windows 持续集成增加源码与扩展 ZIP 的真实归档步骤，覆盖正式发布使用的命令。
- 沿用已配置的 icjunge/gpt-orb-safe 发布源和 Ed25519 公钥，桌面与扩展版本统一为 2.1.1。
- 2.1.0 首次发布构建停在扩展归档步骤，未生成正式 Release；保留原标签，用新补丁版本继续发布。

Windows 实机安装升级与真实账号页面验收仍需完成；构建通过不代表这些验收已完成。

## 2.1.0

- 新增安装版更新：启动后与每 6 小时检查，签名验证后下载，由用户点击「重启并更新」。
- 更新来源与 Ed25519 公钥随程序固定；校验版本、安装包长度、SHA-256 和 SHA-512，拒绝降级及被篡改的文件。
- 安装前再次校验文件并备份本地显示设置。更新发布源尚未配置时保持停用。
- 浏览器扩展迁移到用户数据中的固定目录，完整替换后可在扩展内重新加载，无须每次删除再安装；重载后需要重新配对。
- 加入 GitHub Actions 测试、Windows 安装包构建与独立签名发布工作流。

旧 ZIP 版需要一次迁移安装。Windows 实机安装升级与真实账号页面尚未完成验收；当前版本没有配置正式发布源。

## 2.0.1

- 修复浏览器扩展消息 API 不可用时的未处理错误，显示正确的扩展加载指引。
- 保持原有扩展权限、身份及本机来源校验。
