# 2.5.3 安全设计与已知边界

## 目标与数据流

让桌面悬浮球显示官方 Codex 提供的额度数字，默认来源改为本机官方 Codex CLI，首次须由用户主动开启。2.2 的浏览器模式作为可选备用来源保留。这里说明实现边界，不是零风险承诺或第三方安全认证。

本机模式：悬浮球启动已安装的官方 `codex app-server`，使用标准输入 / 输出完成初始化，再发送 `account/rateLimits/read` 和当前协议支持时的 `account/usage/read` 查询。官方 Codex 使用自己的登录状态访问官方服务；悬浮球只接收并校验额度窗口、时间和有限统计字段。它不启动网络监听型 App Server，也不把 RPC 接口暴露给网页。

浏览器模式：用户选择该来源并完成本机配对后，扩展读取 `https://chatgpt.com/settings/usage?tab=overview` 页面明确标注的可见指标。可选定时模式经用户授权，每轮使用不抢焦点的临时标签页读取 DOM。扩展后台将限制后的数字字段发送到本机 `http://127.0.0.1:43861/v1/usage`；桌面服务再次校验后更新显示。登录仍由浏览器与官方网站管理。

安装版另有独立更新请求：向固定 GitHub Release 来源请求发布描述，验签后下载对应安装包。不把用量、配对码或账号信息放入更新请求。没有自建云端用量同步或额外遥测；CLI 自身的官方网络行为遵循官方工具及本机配置。

## 官方 Codex CLI 的信任边界

- **选择与启动**：官方 CLI 是用户自行安装的外部程序，不随桌面打包。界面仅复制固定安装 / 登录命令或打开官方帮助，不执行安装、登录、退出登录或自动升级。用户首次开启后，启用偏好及分钟间隔长期保存，后续启动按设置继续查询；禁用停止后续查询，不撤销官方登录。
- **凭据归属**：悬浮球不直接读取、复制、解密或自行保存 `auth.json`、Cookie、密码、API Key、设备授权码或令牌。不扫描本机 Codex 会话日志。**官方 CLI 本身仍会读取登录状态，并可能续期或更新凭据**；其文件 / 系统凭据库存储方式取决于官方版本及既有配置，不能承诺必然使用 OS 加密存储。对 CLI 及运行环境的信任是本方案必要条件。
- **请求能力**：只向 App Server 发送固定的初始化、账号状态及用量查询。无任意 RPC 方法、用户输入命令、提示词、线程 / turn、模型任务或自动额度重置入口。只读表示悬浮球不发起这些写操作，不表示官方 CLI 在操作系统层面无任何写入。
- **进程与界面**：CLI 在桌面主进程侧管理，渲染页面不能指定执行路径、命令、RPC 方法或原始参数。所有输出须经过大小 / 时间边界与结构校验，仅有限的数字及状态进入界面；不记录原始 RPC、标准错误或可能含有凭据的底层错误文本。异常显示固定可操作提示。
- **记录语义**：普通查询失败保留该来源上次成功记录与成功时间，不用空响应冒充有效额度；检测到退出登录或账号变化时清除旧账号读数，避免跨账号显示。仅展示官方返回的窗口与重置次数，不编造 Spark 或其他模型独立额度。Token 统计是协议能力允许时返回的服务端范围及最新日桶，不能称作本机今日总数或全部 ChatGPT 用量。统计未知不阻止已有额度显示。
- **来源边界**：本机与浏览器来源由用户明确选择，CLI 失败不自动启动网页或改用浏览器登录。切换后不把两种来源的记录混成一个总量。旧扩展自己的会话任务仍可能继续尝试打开网页；不再使用时请在扩展点击「关闭并撤销授权」。
- **兼容性**：App Server 仍属于实验性接口，CLI 版本或服务端结构变动可能导致读取暂不可用。程序遇到不识别字段显示未知，不从比例猜测 Token；不会为恢复统计自动修改账号配置或扩大权限。

## 页面读取与本机隔离

- **扩展权限**：基础权限为 `activeTab`、`scripting`、`storage`、`alarms`；必需主机权限仅为 `http://127.0.0.1/*`。可选主机权限严格为 `https://chatgpt.com/*`，只在用户主动开启定时模式时申请；拒绝不影响当前页面模式。该授权在浏览器层面覆盖 chatgpt.com 整个来源，精确用量路径限制由代码执行，不应宣称浏览器只授予用量路径。`activeTab` 也属于临时来源级权限。Chrome 主机权限不按端口细分，后台代码和 CSP 进一步固定本机请求端口。没有 `cookies`、`tabs`、`webRequest` 权限，没有复制、解密或保存官方凭据的代码。
- **页面与内容**：每次采集仅接受 `https://chatgpt.com` 下的 `/settings/usage` 或兼容旧版的 `/codex/settings/usage`（可带末尾斜杠和查询参数）。后台、采集器和解析器共同校验；只提取能识别的可见指标，不发送整页文本，不猜测含义不明确的数据。
- **定时与标签页**：间隔为整数 1–1440 分钟，默认 5；只在浏览器运行、官方登录有效、已配对且用户开启时执行。每次创建非活动的临时标签页，不操作已有用量页；只清理本轮创建且未被用户接管的标签页。退出、睡眠、网站验证及限速可能阻止运行，不保证定时精度。启用状态仅保存在会话内，长期设置只保留间隔数字；扩展重载或浏览器重启后不会自动恢复任务。「关闭并撤销授权」会停止任务并调用浏览器权限撤销；即使后台停止消息失败，弹窗也会尝试撤销，并显示未确认的状态。切回当前页面或人工记录模式只停止任务，可能保留授权；浏览器重启后的已有授权也可能保留，下一次点击开启不保证再次弹窗。用户还可在浏览器管理中撤销网站授权。
- **配对**：桌面为当前运行会话生成随机密钥，扩展仅在 `chrome.storage.session` 保存，内容脚本不接触密钥。桌面断开或退出使旧密钥失效；扩展重载、更新或浏览器重启清除会话存储，必须重新配对。
- **本机服务**：只绑定 IPv4 回环地址，校验 Host、扩展 Origin、认证头及结构化数据，限制请求体大小；无文件读取、命令执行或账号访问端点。本机恶意程序可伪造 Origin，因此来源校验不能代替配对密钥。
- **窗口**：渲染进程启用沙箱、上下文隔离，不开放 Node.js；IPC 限定本应用主框架及有限操作。页面和更新错误通过文本显示。
- **记录**：页面读取时间不冒充后台统计更新时间，未知、过期和人工记录均单独标记。倒计时归零不代表官方额度已经恢复。后台刷新失败或完全未识别数值时，不覆盖上次成功快照或伪造成功时间。浏览器来源的 3 分钟历史标记独立于扩展可选刷新间隔。

## 安装版更新的信任边界

1. `update-config.json` 将发布仓库与 Ed25519 公钥随应用固定。普通偏好设置和渲染页面不能指定其他来源或公钥；未配置时不更新。当前仅支持稳定版本的 Windows x64 NSIS 安装包，旧免安装包不启用此流程。
2. 客户端请求固定仓库的 `orb-update.json`，先验证对原始 payload 字节的 Ed25519 签名，再解释版本、标签、文件名、字节长度、SHA-256、SHA-512 与平台。拒绝无效字段、错误签名和低于已安装版本的更新。
3. 网络请求限于 HTTPS、指定 GitHub 发布路径与 GitHub 资源域名，校验重定向，不接受 URL 凭据或自定义端口。发布描述限制为 64 KiB，安装包声明长度限制为 1 GiB。下载器请求移除 Cookie、Authorization 等认证头，不需要 GitHub PAT。
4. `latest.yml` 仅用作下载适配，必须与已签名的发布描述一致。拒绝其他文件、版本、哈希、长度、Web Installer 和附加安装包；停用差分下载和自动退出安装。
5. 下载后校验实际安装包长度及两个哈希。只有此后界面才允许「重启并更新」；点击时再次校验文件，再由安装器执行。普通关闭程序不会安装待更新包。文件检查不能消除同一用户下恶意进程持续修改文件的所有竞态风险。
6. 安装前备份 `preferences.json` 到用户数据目录的 `recovery`；备份失败会停止安装。**这是设置备份，不是完整程序快照或 NSIS 的原子回滚机制。** 安装中断可能需要可信安装包修复及手动恢复设置。
7. 扩展文件由已安装桌面包同步到固定用户数据目录。同步验证清单、权限、固定扩展身份、版本与文件集合，使用临时目录替换并保留恢复副本；同步失败报告错误。2.2 起的校验器仅接受两个完整形状：旧版权限且无可选主机字段，或新权限加精确的可选官方来源；混合形状、额外主机和额外权限均拒绝。可迁移既有旧包并保留备份。2.1.1 等旧桌面校验器遇到新清单会拒绝同步并保留原有文件，可能显示目录更新警告；数字同步协议仍兼容。浏览器端只有本地用户点击才触发重新加载，没有远程脚本或桌面命令触发的远程扩展更新入口。

Ed25519 证明更新包与安装时信任的发布密钥一致，**不是 Windows Authenticode 证书**，也不能使恶意发布者变可信。当前没有 Windows 发布者签名。第一次安装的来源、公钥锚点和发布者私钥保护仍然关键：攻击者若控制发布私钥，可能签发恶意更新；攻击者若同时替换首次安装包及其哈希，单独比对哈希也无法证明身份。

## 发布流程

- 依赖版本固定并提交 lockfile；GitHub Actions 第三方步骤固定完整提交 SHA。构建与签名分为两个作业，构建作业无发布签名私钥。
- 发布者在仓库外生成 Ed25519 私钥，将公钥及实际仓库配置审核后提交。签名作业读取 `release` 环境中的 `ORB_UPDATE_PRIVATE_KEY`，校验私钥对应公钥与应用固定值一致，再签实际构建产物；私钥不写入安装包、源码或发布资产。
- 新版本提交可自动准备精确提交的版本标签并启动构建；签名与正式公开发布必须通过原 `release` 环境审批。不会更换公钥、覆盖既有标签或移除保护规则。`release` 环境的审批及其他保护规则需要维护者在 GitHub 中设置，仅写入环境名称不会自动创建审批保护。
- 更新资产须可匿名下载；当前工作流面向公开仓库发布。不允许通过在客户端嵌入 PAT 来绕过私有仓库限制。私钥丢失、泄漏或仓库迁移需要单独审核迁移方案，不静默更换信任锚点。

具体命令与资产清单见 [RELEASE.md](RELEASE.md)。

## 剩余风险与验证范围

本机、CLI 或浏览器已经被控制时，攻击者可能替换可执行程序、读取运行内存、窃取配对码、修改用户目录中的扩展，或直接操作官方登录；工具无法防御这类系统级控制。配对码仅授权用量数字上报，不提供 GPT 账号访问能力；泄漏后可在桌面断开并重新配对。

扩展未上架商店，需开发者模式加载。权限较少并不等于可以信任任意来源的扩展。页面改版、国际化文案、企业页面、浏览器限速及统计延迟可能让数据缺失或失效，官方页面是核对依据；不保证每个账号都公开 Token 指标。

测试使用合成数据，覆盖 CLI 固定协议、字段校验、异常处理、来源及刷新状态，以及 Node 接收边界、签名与更新状态、安装包篡改、设置备份。已有 DOM、离屏界面和 Electron Chromium MV3 传输检查，不等于完整 Chrome / Edge 工具栏、`activeTab` 及本地网络权限验收。实体 Windows 安装 / 升级、真实官方 CLI 登录与额度响应、真实账号页面尚需验收。模拟 RPC 不使用用户账号，也不能替代实机结论。

2.x 不读取、复用或删除 1.0.0 的账号凭据。此前授权需要单独通过可靠退出或 OpenAI 官方账号会话管理处理。请勿在本工具填写账号密码、官方设备授权代码、API Key 或访问令牌。

## 参考

- 官方 Codex CLI：<https://learn.chatgpt.com/docs/codex/cli>
- 官方认证：<https://learn.chatgpt.com/docs/auth>
- 官方 App Server：<https://learn.chatgpt.com/docs/app-server>

- Chrome `activeTab`：<https://developer.chrome.com/docs/extensions/develop/concepts/activeTab>
- 扩展跨来源请求：<https://developer.chrome.com/docs/extensions/develop/concepts/network-requests>
- 可选权限：<https://developer.chrome.com/docs/extensions/reference/api/permissions>
- 扩展定时：<https://developer.chrome.com/docs/extensions/reference/api/alarms>
- 扩展会话存储：<https://developer.chrome.com/docs/extensions/reference/api/storage>
- Electron 安全建议：<https://www.electronjs.org/docs/latest/tutorial/security>
- electron-builder 自动更新：<https://www.electron.build/docs/features/auto-update/>
